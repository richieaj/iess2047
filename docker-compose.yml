version: "3"
services:
  app:
    build: .
    command: anvil-app-server --app IESS2047 --data-dir /anvil-data
    ports:
      - 3030:3030
    volumes:
      - ./:/apps/IESS2047
    #restart: always
